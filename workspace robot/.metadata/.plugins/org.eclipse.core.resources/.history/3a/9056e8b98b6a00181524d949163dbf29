package de.fynn.training.controlled;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.util.Scanner;

public class SmallTalkKI  {
    static boolean var = false;
   public static Scanner scan = new Scanner(System.in);
	public static void main(String[] args) throws IOException{
		
		InputStreamReader isr = new InputStreamReader(System.in);
		BufferedReader br = new BufferedReader(isr);
		
		do{  
			System.out.print("Sag etwas zu James:");
	        String input = br.readLine();
	     if(input.equalsIgnoreCase("hi")) { 
			System.out.println("Hallo wie geht es dir?"); 
			var = false;
			input= br.readLine();
		     if(input.equalsIgnoreCase("Gut")) {
		    	System.out.println("Das gefällt mir");
		     }else if(input.equalsIgnoreCase("schlecht")) {
		    	System.out.println("Schade! Was bedrückt dich denn ?");
		    	System.out.print("Sag etwas zu James:");
		    	input = br.readLine();
		         if(input.equals("var")) {
		        	 System.out.println("var");
		        	
		         
		       
		         }   
				  
		     }
	     }
	     else if (input.equals("Wie spät ist es?")) {
	    	 System.out.println("Es ist " + Clock.getTime() +" Uhr!");
	     } else if(input.equals("Stelle Wecker")) {
	    	  System.out.println("Wie spät?");
	    	AlarmClock.alarm(scan.nextLine());
	    	System.out.println("Wecker wurde gestellt!");
	    	  
	     } else if(input.equals("Stelle Timer")) {
	    	 System.out.println("Wie lange?");
	    	 String text = Timer.scan.nextLine();
	    	 Scanner s = new Scanner(text);
	    	 int time = s.nextInt();
	    	 String t;
	    	 int secs = LocalTime.now().getHour() ;
	    	 int minutes = LocalTime.now().getMinute() ;
	    	 int hours = LocalTime.now().getSecond();
	    	

	    	 
             // Seconds
	    	 if(text.contains(Timer.seconds) || text.contains(Timer.second)) {
	    			
	    			int h = LocalTime.now().getHour();
	    			int m = LocalTime.now().getMinute();
	    			int sec = LocalTime.now().getSecond();
	    			//int d = scann.nextInt();
	               
	    			String timeall = h + ":" + m + ":" + sec + time;
	    			System.out.println(sec);
	    			Timer.alarm(timeall);
		    		}
	    	 

	    	 
	    	 // Minutes
	    	 
	    	 if(text.contains(Timer.minutes) || text.contains(Timer.minute)) {
	    		 Scanner sc = new Scanner(Clock.getTimeHours());
	    			Scanner sca = new Scanner(Clock.getTimeMinutes());
	    			Scanner scanner = new Scanner(Clock.getTimeSeconds());
	    			Scanner scann = new Scanner(Clock.getDay());
	    			
	    			int h = sc.nextInt();
	    			int min = sca.nextInt();
	    			int stime = scanner.nextInt();
	    			int d = scann.nextInt();

		    		minutes = min + time;
		    		if(minutes >= 60){
		    			h ++;
		    			minutes = minutes - 60;
		    		}
		    		if(minutes < 10) {
		    			String timeall = h + ":" + minutes + ":" + stime;
		    			Timer.alarm(timeall);
		    		}
		    		if(minutes >= 10) {
		    			String timeall = h + ":0" + minutes + ":" + stime;
		    			Timer.alarm(timeall);
		    		}
		    		}
	    	 
	    	 //Stunden
	         	 if(text.contains(Timer.hours) || text.contains(Timer.hour)) {
	         		 
	         		Scanner sc = new Scanner(Clock.getTimeHours());
	         		Scanner sca = new Scanner(Clock.getTimeMinutes());
	         		Scanner scanner = new Scanner(Clock.getTimeSeconds());
	         		Scanner scann = new Scanner(Clock.getDay());
	         	
	    			
	         		int hou = sc.nextInt();
	         		int mintime = sca.nextInt();
	         		int stime = scanner.nextInt();
	         		int d = scann.nextInt();

		    		hours = hou + time;
		    		if(hours >= 24){
		    			d ++;
		    			hours = hours - 60;
		    		}
		    		if(hours < 10){
		    		String timeall = d + ":0" + hours + ":" + mintime + ":" + stime;
		    		Timer.alarm(timeall);
		    	} if(hours >= 10) {
		    		String timeall = d + ":" + hours + ":" + mintime + ":" + stime;
		    		Timer.alarm(timeall);
		    	}
	     }
	     }
	    	
	     
	    	 else{
	    	 System.out.println("Wie bitte? Kannst du das bitte nocheinmal wiederholen?");
	     } 
	     }
	while(var == false);
		}
	}




